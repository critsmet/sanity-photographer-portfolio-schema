"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _react = _interopRequireDefault(require("react"));

var _functions = _interopRequireDefault(require("part:@sanity/form-builder/input/array/functions"));

var _operators = require("rxjs/operators");

var _resolveTypeName = require("../../utils/resolveTypeName");

var _pathUtils = require("../../utils/pathUtils");

var _UploadTargetFieldset = _interopRequireDefault(require("../../utils/UploadTargetFieldset"));

var _PatchEvent = require("../../PatchEvent");

var _ArrayInput = _interopRequireDefault(require("./styles/ArrayInput.css"));

var _resolveListComponents = _interopRequireDefault(require("./resolveListComponents"));

var _ItemValue = _interopRequireDefault(require("./ItemValue"));

var _randomKey = _interopRequireDefault(require("./randomKey"));

var _default = _interopRequireDefault(require("part:@sanity/components/buttons/default"));

var _default2 = _interopRequireDefault(require("part:@sanity/components/fieldsets/default"));

var _Details = _interopRequireDefault(require("../common/Details"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance"); }

function _iterableToArrayLimit(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function createProtoValue(type) {
  if (type.jsonType !== 'object') {
    throw new Error(`Invalid item type: "${type.type}". Default array input can only contain objects (for now)`);
  }

  const key = (0, _randomKey.default)(12);
  return type.name === 'object' ? {
    _key: key
  } : {
    _type: type.name,
    _key: key
  };
}

class ArrayInput extends _react.default.Component {
  constructor(...args) {
    super(...args);

    _defineProperty(this, "_element", void 0);

    _defineProperty(this, "uploadSubscriptions", void 0);

    _defineProperty(this, "uploadSubscriptions", {});

    _defineProperty(this, "state", {
      isMoving: false
    });

    _defineProperty(this, "insert", (itemValue, position, atIndex) => {
      const onChange = this.props.onChange;
      onChange(_PatchEvent.PatchEvent.from((0, _PatchEvent.setIfMissing)([]), (0, _PatchEvent.insert)([itemValue], position, [atIndex])));
    });

    _defineProperty(this, "handlePrepend", value => {
      this.insert(value, 'before', 0);
      this.handleFocusItem(value);
    });

    _defineProperty(this, "handleAppend", value => {
      this.insert(value, 'after', -1);
      this.handleFocusItem(value);
    });

    _defineProperty(this, "handleRemoveItem", item => {
      this.removeItem(item);
    });

    _defineProperty(this, "handleFocus", () => {
      this.props.onFocus([_pathUtils.FOCUS_TERMINATOR]);
    });

    _defineProperty(this, "handleFocusItem", item => {
      this.props.onFocus([{
        _key: item._key
      }, _pathUtils.FOCUS_TERMINATOR]);
    });

    _defineProperty(this, "handleItemChange", (event, item) => {
      const _this$props = this.props,
            onChange = _this$props.onChange,
            value = _this$props.value;
      const memberType = this.getMemberTypeOfItem(item);

      if (!memberType) {
        // eslint-disable-next-line no-console
        console.log('Could not find member type of item ', item);
        return;
      }

      if (memberType.readOnly) {
        return;
      }

      const key = item._key || (0, _randomKey.default)(12);
      onChange(event.prefixAll({
        _key: key
      }).prepend(item._key ? [] : (0, _PatchEvent.set)(key, [value.indexOf(item), '_key'])));
    });

    _defineProperty(this, "handleSortStart", () => {
      this.setState({
        isMoving: true
      });
    });

    _defineProperty(this, "handleSortEnd", event => {
      this.setState({
        isMoving: false
      });
      const _this$props2 = this.props,
            value = _this$props2.value,
            onChange = _this$props2.onChange;
      const item = value[event.oldIndex];
      const refItem = value[event.newIndex]; // console.log('from %d => %d', event.oldIndex, event.newIndex, event)

      if (!item._key || !refItem._key) {
        // eslint-disable-next-line no-console
        console.error('Neither the item you are moving nor the item you are moving to have a key. Cannot continue.');
        return;
      }

      if (event.oldIndex === event.newIndex || item._key === refItem._key) {
        return;
      }

      onChange(_PatchEvent.PatchEvent.from((0, _PatchEvent.unset)([{
        _key: item._key
      }]), (0, _PatchEvent.insert)([item], event.oldIndex > event.newIndex ? 'before' : 'after', [{
        _key: refItem._key
      }])));
    });

    _defineProperty(this, "renderList", () => {
      const _this$props3 = this.props,
            type = _this$props3.type,
            markers = _this$props3.markers,
            readOnly = _this$props3.readOnly,
            value = _this$props3.value,
            focusPath = _this$props3.focusPath,
            onBlur = _this$props3.onBlur,
            onFocus = _this$props3.onFocus,
            level = _this$props3.level,
            filterField = _this$props3.filterField;
      const isMoving = this.state.isMoving;
      const options = type.options || {};
      const hasMissingKeys = value.some(item => !item._key);
      const isSortable = options.sortable !== false && !hasMissingKeys;
      const isGrid = options.layout === 'grid';

      const _resolveListComponent = (0, _resolveListComponents.default)(isSortable, isGrid),
            List = _resolveListComponent.List,
            Item = _resolveListComponent.Item;

      const listProps = isSortable ? {
        movingItemClass: _ArrayInput.default.movingItem,
        onSortEnd: this.handleSortEnd,
        onSortStart: this.handleSortStart,
        lockToContainerEdges: true,
        useDragHandle: !isGrid
      } : {};
      const listItemClassName = isMoving ? _ArrayInput.default.listItemMute : _ArrayInput.default.listItem;
      return _react.default.createElement(List, _extends({
        className: readOnly ? _ArrayInput.default.listReadOnly : _ArrayInput.default.list
      }, listProps), value.map((item, index) => {
        const isChildMarker = marker => (0, _pathUtils.startsWith)([index], marker.path) || (0, _pathUtils.startsWith)([{
          _key: item && item._key
        }], marker.path);

        const itemProps = isSortable ? {
          index
        } : {};
        return _react.default.createElement(Item, _extends({
          key: item._key,
          className: isGrid ? _ArrayInput.default.gridItem : listItemClassName
        }, itemProps), _react.default.createElement(_ItemValue.default, {
          type: type,
          value: item,
          level: level,
          markers: markers.filter(isChildMarker),
          onRemove: this.handleRemoveItem,
          onChange: this.handleItemChange,
          focusPath: focusPath,
          filterField: filterField,
          onFocus: onFocus,
          readOnly: readOnly || hasMissingKeys,
          onBlur: onBlur
        }));
      }));
    });

    _defineProperty(this, "setElement", el => {
      this._element = el;
    });

    _defineProperty(this, "getUploadOptions", file => {
      const _this$props4 = this.props,
            type = _this$props4.type,
            resolveUploader = _this$props4.resolveUploader;

      if (!resolveUploader) {
        return [];
      }

      return type.of.map(memberType => {
        const uploader = resolveUploader(memberType, file);
        return uploader && {
          type: memberType,
          uploader
        };
      }).filter(Boolean);
    });

    _defineProperty(this, "handleFixMissingKeys", () => {
      const _this$props5 = this.props,
            onChange = _this$props5.onChange,
            value = _this$props5.value;
      const patches = value.map((val, i) => (0, _PatchEvent.setIfMissing)((0, _randomKey.default)(), [i, '_key']));
      onChange(_PatchEvent.PatchEvent.from(...patches));
    });

    _defineProperty(this, "handleUpload", ({
      file,
      type,
      uploader
    }) => {
      const onChange = this.props.onChange;
      const item = createProtoValue(type);
      const key = item._key;
      this.insert(item, 'after', -1);
      const events$ = uploader.upload(file, type).pipe((0, _operators.map)(uploadEvent => _PatchEvent.PatchEvent.from(uploadEvent.patches).prefixAll({
        _key: key
      })));
      this.uploadSubscriptions = _objectSpread({}, this.uploadSubscriptions, {
        [key]: events$.subscribe(onChange)
      });
    });
  }

  removeItem(item) {
    const _this$props6 = this.props,
          onChange = _this$props6.onChange,
          onFocus = _this$props6.onFocus,
          value = _this$props6.value;
    onChange(_PatchEvent.PatchEvent.from((0, _PatchEvent.unset)(item._key ? [{
      _key: item._key
    }] : [value.indexOf(item)])));

    if (item._key in this.uploadSubscriptions) {
      this.uploadSubscriptions[item._key].unsubscribe();
    }

    const idx = value.indexOf(item);
    const nextItem = value[idx + 1] || value[idx - 1];
    onFocus([nextItem ? {
      _key: nextItem._key
    } : _pathUtils.FOCUS_TERMINATOR]);
  }

  getExpandedItem() {
    const _this$props7 = this.props,
          focusPath = _this$props7.focusPath,
          value = _this$props7.value;

    const _ref = focusPath || [],
          _ref2 = _slicedToArray(_ref, 1),
          head = _ref2[0];

    return head && value.find(item => item._key === head._key);
  }

  getMemberTypeOfItem(item) {
    const type = this.props.type;
    const itemTypeName = (0, _resolveTypeName.resolveTypeName)(item);
    return type.of.find(memberType => memberType.name === itemTypeName);
  }

  focus() {
    if (this._element) {
      this._element.focus();
    }
  }

  render() {
    const _this$props8 = this.props,
          type = _this$props8.type,
          level = _this$props8.level,
          markers = _this$props8.markers,
          readOnly = _this$props8.readOnly,
          onChange = _this$props8.onChange,
          value = _this$props8.value;
    const hasMissingKeys = (value || []).some(item => !item._key);

    if (hasMissingKeys) {
      return _react.default.createElement(_default2.default, {
        legend: type.title,
        description: type.description,
        level: level,
        tabIndex: 0,
        onFocus: this.handleFocus,
        ref: this.setElement,
        markers: markers
      }, _react.default.createElement("div", {
        className: _ArrayInput.default.missingKeysWarning
      }, "Some items in this list are missing their keys. We need to fix this before the list can be edited.", _react.default.createElement("div", {
        className: _ArrayInput.default.fixMissingKeysButtonWrapper
      }, _react.default.createElement(_default.default, {
        color: "primary",
        onClick: this.handleFixMissingKeys
      }, "Fix missing keys")), _react.default.createElement(_Details.default, {
        title: _react.default.createElement("b", null, "Why is this happening?")
      }, "This usually happens when items are created through the API client from outside the Content Studio and someone forgets to set the ", _react.default.createElement("code", null, "_key"), "-property of list items.")), this.renderList());
    }

    return _react.default.createElement(_UploadTargetFieldset.default, {
      markers: markers,
      tabIndex: 0,
      legend: type.title,
      description: type.description,
      level: level,
      className: _ArrayInput.default.root,
      onUpload: this.handleUpload,
      onFocus: this.handleFocus,
      type: type,
      getUploadOptions: this.getUploadOptions,
      ref: this.setElement
    }, value && value.length > 0 && this.renderList(), _react.default.createElement(_functions.default, {
      type: type,
      value: value,
      readOnly: readOnly,
      onAppendItem: this.handleAppend,
      onPrependItem: this.handlePrepend,
      onFocusItem: this.handleFocusItem,
      onCreateValue: createProtoValue,
      onChange: onChange
    }));
  }

}

exports.default = ArrayInput;

_defineProperty(ArrayInput, "defaultProps", {
  focusPath: []
});