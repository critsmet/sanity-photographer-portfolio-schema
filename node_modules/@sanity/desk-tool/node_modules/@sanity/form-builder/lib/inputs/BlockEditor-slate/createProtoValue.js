"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createProtoValue = createProtoValue;

var _randomKey = _interopRequireDefault(require("./util/randomKey"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createProtoValue(type, key = undefined) {
  if (type.jsonType !== 'object') {
    throw new Error(`Invalid item type: "${JSON.stringify(type.jsonType)}". Block editor can only contain objects (for now)`);
  }

  return {
    _type: type.name,
    _key: key || (0, _randomKey.default)(12)
  };
}