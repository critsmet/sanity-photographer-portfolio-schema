"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.EmailType = void 0;

var _pick2 = _interopRequireDefault(require("lodash/pick"));

var _primitivePreview = _interopRequireDefault(require("../preview/primitivePreview"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const OVERRIDABLE_FIELDS = ['jsonType', 'type', 'name', 'title', 'description', 'options'];
const EMAIL_CORE = {
  name: 'email',
  title: 'Email',
  type: null,
  jsonType: 'string'
};
const EmailType = {
  get() {
    return EMAIL_CORE;
  },

  extend(subTypeDef) {
    const parsed = Object.assign((0, _pick2.default)(EMAIL_CORE, OVERRIDABLE_FIELDS), subTypeDef, {
      type: EMAIL_CORE,
      preview: _primitivePreview.default
    });
    return subtype(parsed);

    function subtype(parent) {
      return {
        get() {
          return parent;
        },

        extend: extensionDef => {
          const current = Object.assign({}, parent, (0, _pick2.default)(extensionDef, OVERRIDABLE_FIELDS), {
            type: parent
          });
          return subtype(current);
        }
      };
    }
  }

};
exports.EmailType = EmailType;