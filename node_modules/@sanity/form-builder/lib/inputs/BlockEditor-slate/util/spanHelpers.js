"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSpanType = getSpanType;
exports.getSpansField = getSpansField;
exports.getBlockField = getBlockField;
exports.openSpanDialog = openSpanDialog;

var _findComponentFromDOMNode = _interopRequireDefault(require("./findComponentFromDOMNode"));

var _slate = require("slate");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getSpanType(blockArrayType) {
  return getSpansField(blockArrayType).type.of.find(type => type.name === 'span');
}

function getSpansField(blockArrayType) {
  return getBlockField(blockArrayType).fields.find(field => field.name === 'spans');
}

function getBlockField(blockArrayType) {
  return blockArrayType.of.find(ofType => ofType.type.name === 'block');
} // Opens a span components editing dialog from a Slate node input


function openSpanDialog(spanNode) {
  const component = (0, _findComponentFromDOMNode.default)((0, _slate.findDOMNode)(spanNode));
  component.setState({
    isEditing: true
  });
}