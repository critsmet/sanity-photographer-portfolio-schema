"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _omit2 = _interopRequireDefault(require("lodash/omit"));

var _react = _interopRequireWildcard(require("react"));

var _semverCompare = _interopRequireDefault(require("semver-compare"));

var _sanityVersions = _interopRequireDefault(require("sanity:versions"));

var _fullscreen = _interopRequireDefault(require("part:@sanity/components/dialogs/fullscreen?"));

var _client = _interopRequireDefault(require("part:@sanity/base/client"));

var _FullscreenError = _interopRequireDefault(require("./FullscreenError"));

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = Object.defineProperty && Object.getOwnPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : {}; if (desc.get || desc.set) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const fakeOutdatedModule = false;
const fakeOutdatedModuleSeverity = 'high';

let applySeverity = inp => inp;

if (fakeOutdatedModule) {
  _sanityVersions.default['@sanity/base'] = '0.118.0';

  applySeverity = inp => {
    const mod = inp && inp.outdated.find(item => item.name === '@sanity/base') || {};
    mod.severity = fakeOutdatedModuleSeverity;
    return inp;
  };
}

const onIdle = typeof window.requestIdleCallback === 'function' ? window.requestIdleCallback : cb => setTimeout(cb, 0); // eslint-disable-next-line id-length

const buildQueryString = () => ({
  m: Object.keys(_sanityVersions.default).map(pkg => `${pkg}@${_sanityVersions.default[pkg]}`)
});

const hashQuery = items => items.join(',').replace(/@?sanity[/-]/g, '');

const storage = typeof sessionStorage === 'undefined' ? {} : sessionStorage;

const onVersionCheckError = err => {
  // eslint-disable-next-line no-console
  console.warn('Module versions check failed. Dependencies *might* be out of date.', err);
};

const breakify = lines => {
  const nodes = [];

  for (let i = 0; i < lines.length; i++) {
    nodes.push(lines[i]);

    if (i !== lines.length - 1) {
      nodes.push(_react.default.createElement("br", {
        key: i
      }));
    }
  }

  return nodes;
};

const paragraphify = text => {
  return text.split('\n\n').map((para, i) => {
    const lines = para.split('\n'); // eslint-disable-next-line react/no-array-index-key

    return _react.default.createElement("p", {
      key: i
    }, breakify(lines));
  });
};

const getLatestInstalled = () => {
  const versionNums = Object.keys(_sanityVersions.default).map(pkg => _sanityVersions.default[pkg]);
  const sorted = versionNums.sort(_semverCompare.default);
  return sorted[sorted.length - 1];
};

const checkVersions = (options = {}) => {
  const getOutdated = options.getOutdated;
  const query = buildQueryString();
  const hash = hashQuery(query.m);
  const local = storage.versionCheck && storage.versionCheck.indexOf(hash) === 0 && storage.versionCheck.slice(hash.length + 1);

  if (!getOutdated && local) {
    return Promise.resolve({
      result: JSON.parse(local)
    });
  }

  return _client.default.request({
    uri: '/versions',
    query: buildQueryString(),
    json: true
  }).then(result => ({
    hash,
    result: applySeverity(result)
  }));
};

class VersionChecker extends _react.PureComponent {
  constructor(...args) {
    super(...args);
    this.state = {};
    this.onResponse = this.onResponse.bind(this);
    this.handleClose = this.handleClose.bind(this);
  }

  onResponse(res) {
    // Don't include outdated modules in the stored result
    const result = (0, _omit2.default)(res.result, ['outdated']);

    if (result.hash && storage) {
      storage.versionCheck = [res.hash, JSON.stringify(result)].join('|');
    } // If we have unsupported modules, we want to show a dialog


    if (!result.isSupported) {
      this.setState({
        result
      });
    }

    if (__DEV__ && res.result && res.result.outdated) {
      const modules = res.result.outdated.map(mod => mod.name).join('\n  - ');
      const instructions = 'Run `sanity upgrade` to update them'; // eslint-disable-next-line no-console

      console.warn(`The following modules are outdated:\n  - ${modules}\n\n${instructions}`);
    }
  }

  handleClose() {
    this.setState({
      result: null
    });
  }

  componentDidMount() {
    onIdle(() => {
      checkVersions().then(this.onResponse).catch(onVersionCheckError);
    });
  }

  render() {
    const result = this.state.result;

    if (!result || result.isSupported || result.isSupported === undefined) {
      return null;
    }

    const Dialog = _fullscreen.default || _FullscreenError.default;
    const title = 'Unsupported module versions';
    return _react.default.createElement(Dialog, {
      centered: true,
      isOpen: true,
      color: "danger",
      title: title,
      onClose: this.handleClose
    }, paragraphify(result.message || ''), result.helpUrl && _react.default.createElement("p", null, "For more information, please read ", _react.default.createElement("a", {
      href: result.helpUrl
    }, result.helpUrl)));
  }

}

VersionChecker.checkVersions = checkVersions;
VersionChecker.getLatestInstalled = getLatestInstalled;
var _default = VersionChecker;
exports.default = _default;