export interface Rules {
    [key: string]: {
        minWidth?: number;
        maxWidth?: number;
        minHeight?: number;
        maxHeight?: number;
    };
}
export default function matchQueries(rules: Rules): ({height, width}: {
    height: number;
    width: number;
}) => {
    [key: string]: boolean;
};
